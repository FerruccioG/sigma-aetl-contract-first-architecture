# SIGMA-AETL Validator — Contract v1.0 (FROZEN)
# This service enforces the authoritative ingestion contract.
# Do not upgrade dependencies without a version bump.
# Python >= 3.10 required
# Recommended runtime: uvicorn --workers 1 (validator is CPU-light)
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
